
class Location {

    constructor(dir) {
        this.directory = dir;
    }

    navigate() {

        return new Promise((resolve, reject) => {
            try {
                
            } catch (err) {reject(err);};
        });

    }

}
